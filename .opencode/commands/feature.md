---
description: Coordina una funcionalidad mediante etapas autorizadas y revisables.
agent: noryven-cto
subtask: false
---

Gestiona la siguiente solicitud de funcionalidad para ClipAI:

$ARGUMENTS

Aplica este flujo como etapas separadas:

explorar -> proponer plan -> solicitar o comprobar autorizacion -> construir ->
probar -> revisar -> documentar -> traspasar

No ejecutes automaticamente todas las etapas. La autorizacion para explorar o
planificar no autoriza editar. Antes de construir, comprueba que Sofia aprobo el
plan y los archivos; antes de documentar, comprueba la autorizacion documental.
Delega cada etapa solo al especialista correspondiente y como maximo una vez.
Si falta una autorizacion, detente con el plan, los riesgos y la pregunta exacta
que debe resolver Sofia.

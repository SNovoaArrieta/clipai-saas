{
  "name": "@clipai/client",
  "version": "0.0.0",
  "private": true,
  "description": "Reserved workspace for the future ClipAI frontend.",
  "scripts": {
    "typecheck": "tsc --version && node -e \"console.log('[SKIPPED] Client typecheck is not configured: the React application has not been created yet.')\"",
    "test": "node -e \"console.log('[SKIPPED] Client tests are not configured: no application tests were run.')\"",
    "build": "node -e \"console.log('[SKIPPED] Client build is not configured: the application has not been created yet.')\""
  }
}
